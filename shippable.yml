
language: node_js

node_js:
  - 6.9.4

branches:
  only:
    - eocoe
    - eocoe-beta
    - lidar
    - lidar-beta

build:
  ci:
    # tenant-specific configuration
    - EBENV=$(echo $BRANCH | cut -d'-' -f 1) # get e.g. 'lidar' from 'lidar-beta'
    - cp -f ./app.client/config/config.$EBENV.ts ./app.client/config/config.ts
    - cp -f ./app.client/styles/config.$EBENV.ts ./app.client/styles/config.ts
    - cp -f ./app.client/images/favicon.$EBENV.png ./app.client/images/favicon.png
    - cp -f ./app.server/config/config.$EBENV.ts ./app.server/config/config.ts
    - cp -f ./app.server/data/data.$EBENV.ts ./app.server/data/data.ts
    # build
    - yarn install
    - yarn run build:prod
    # workaround https://github.com/Shippable/support/issues/3304
    - pip install --upgrade botocore
    - pip install setuptools==34.0.1

integrations:
  deploy:
    - integrationName: "aws-jncc-prod-subscription-integration"
      type: aws
      target: eb_paas
      platform: "Node.js"
      application_name: deli
      env_name: deli-$BRANCH # deploy to branch environment e.g. 'deli-lidar-beta'
      region: "eu-west-1"

